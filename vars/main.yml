#SPDX-License-Identifier: MIT-0
---
# vars file for ansible_ready

ansible_ready__os_const:
  install_packages:
    debian: [python3, python3-pip]
    alpine: [python3, py3-pip]
    suse: [python3, python3-pip]
    archlinux: [python, python-pip]
    redhat: [python3, python3-pip]
